[Service]
ExecStart=
ExecStart=/bin/bash -lc 'source /home/ubuntu/CrediClip/.venv/bin/activate && python scripts/queue_worker.py --api-base "${API_BASE:-https://crediclip-axraza-msba.fly.dev}" --worker-id "${WORKER_ID:-oracle-worker-1}" --ingest-mode "${INGEST_MODE:-rich}" --analysis-mode "${ANALYSIS_MODE:-local}" --poll-sec "${POLL_SEC:-3}" --timeout-sec "${TIMEOUT_SEC:-180}" --exclude-platforms youtube_shorts'
